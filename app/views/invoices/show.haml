!!!
%html
  %head
    %title= @title
  %body
    %div{style: 'font-family: "Helvetica Neue", Helvetica, Arial, sans-serif; padding: 20px; border: 1px solid #ccc; border-radius: 10px;'}

      %div{style: "float: left; width: 75%; padding-right: 5%;"}
        %h2= @title
        %p
          For services provided between
          %strong #{@invoice.date_start.strftime("%A")}, #{@invoice.date_start}
          and
          %strong #{@invoice.date_end.strftime("%A")}, #{@invoice.date_end}

      %div{style: "float: left; width: 20%;"}
        %p{style: "white-space: nowrap;"}
          <em>From</em> <br>
          <strong>Topher Hunt</strong><br>
          15 Mary Jane Ln<br>
          Florence, MA 01062<br>
          (802) 236 6073

        %p
          <em>Billed to</em> <br>
          <strong>#{@invoice.client.name}</strong>

      %br{style: "clear: both;"}

      %table
        %tr
          %td{style: "padding: 5px;"} <strong>Date</strong>
          %td{style: "padding: 5px;"} <strong>Project</strong>
          %td{style: "padding: 5px;"} <strong>Description</strong>
          %td{style: "padding: 5px;"} <strong>Rate</strong>
          %td{style: "padding: 5px;"} <strong>Hours</strong>
          %td{style: "padding: 5px;"} <strong>Cost</strong>

        - @invoice.work_entries.each do |entry|
          %tr{style: "background-color: ##{cycle "fff", "f6f6f6"};"}
            %td{style: "padding: 5px; white-space: nowrap;"}= date entry.date
            %td{style: "padding: 5px; white-space: nowrap;"}= entry.project.name
            %td{style: "padding: 5px;"}= entry.invoice_notes
            %td{style: "padding: 5px;"}= @invoice.rate.format
            %td{style: "padding: 5px;"}= entry.duration
            %td{style: "padding: 5px;"}= (@invoice.rate * entry.duration).format

        %tr
          %td &nbsp;
        %tr
          %td
          %td
          %td
          %td <strong>Total</strong>
          %td <strong>#{@invoice.total_hours}</strong>
          %td <strong>#{(@invoice.rate * @invoice.total_hours).format}</strong>
